This is an API with frontend which deals with the products management.
To run it propertly first you should open you terminal and install Json-server if you haven't installed it by using the command: npm install -g json-server
Then Start the json-server to mock your API. Open your terminal in the project directory and run: json-server --watch db.json --port 3000
